Oldairy 1.0.3 - Released on October 13th, 2023.

Additions:

* N/a

Changes:

* Disable `package_info_plus`, until better solution is found;
* Downgrade `path_provider_android` from `2.0.33` to `2.0.7`;
* Downgrade `targetSdkVersion` from `33` to `29`;
* Downgrade `compileSdkVersion` from `33` to `29`.

Fixes:

* N/a.
